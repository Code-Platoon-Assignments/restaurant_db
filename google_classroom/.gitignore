node_modules

# probable name of oauth credentials file
creds.json

# access token generated by `clasp login --creds creds.json
.clasprc.json

# .js files should be generated in the google cloud environment by typescript.
# this will happen when you run `clasp push`. We don't need them in version control
# and pushing a .js file if it already exists in the cloud will throw an error so we should
# never really check these into version control
*.js